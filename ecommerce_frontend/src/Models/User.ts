export type UserProfileToken = {
    role: string;
    email: string;
    name: string;
    token: string;
  };

  export type UserProfile = {
    role: string;
    email: string;
    name: string;
  };