export type UserProfileToken = {
    customerId: number
    role: string;
    email: string;
    name: string;
    token: string;
  };

  export type UserProfile = {
    customerId: number
    role: string;
    email: string;
    name: string;
  };